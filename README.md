# برنامه اتوماسیون ورود به ایتا

این برنامه به صورت خودکار مرورگر کروم را باز کرده، وارد وب پیام‌رسان ایتا می‌شود، شماره تلفن را وارد می‌کند و روی دکمه ادامه کلیک می‌کند.

## ویژگی‌ها

- باز کردن خودکار مرورگر Google Chrome
- رفتن به آدرس https://web.eitaa.com/
- وارد کردن شماره تلفن به صورت خودکار (9135621232)
- کلیک خودکار روی دکمه "ادامه"
- گزارش‌دهی مراحل به زبان انگلیسی در کنسول
- مدیریت خودکار ChromeDriver (نیازی به دانلود دستی نیست)

## نیازمندی‌ها

### سیستم عامل
- **ویندوز 10 یا 11** (این برنامه برای ویندوز طراحی شده است)
- Linux و macOS نیز پشتیبانی می‌شوند

### نرم‌افزارهای مورد نیاز
- **Python 3.11** یا بالاتر ([دانلود Python](https://www.python.org/downloads/))
- **Google Chrome** نصب شده روی سیستم ([دانلود Chrome](https://www.google.com/chrome/))

### کتابخانه‌های Python
- selenium
- webdriver-manager

## نصب و راه‌اندازی

### روش 1: نصب خودکار (پیشنهادی) ⭐

#### در ویندوز:
1. فایل `setup.bat` را دوبار کلیک کنید
2. برنامه به صورت خودکار همه چیز را نصب می‌کند

#### در لینوکس/macOS:
```bash
python3 setup.py
```
یا
```bash
python setup.py
```

💡 **نکته:** در برخی سیستم‌ها باید از `python3` به جای `python` استفاده کنید.

### روش 2: نصب دستی

#### مرحله 1: نصب Python
1. Python 3.11 را از [python.org](https://www.python.org/downloads/) دانلود کنید
2. هنگام نصب، حتماً گزینه "Add Python to PATH" را فعال کنید

#### مرحله 2: نصب کتابخانه‌ها
در Command Prompt یا PowerShell دستور زیر را اجرا کنید:

```bash
pip install -r requirements.txt
```

## اجرای برنامه

### با شماره پیش‌فرض (9135621232):
```bash
python eitaa_automation.py
```

### با شماره دلخواه:
```bash
python eitaa_automation.py 9101234567
```

💡 **نکته:** شماره را بدون کد کشور (+98) و فقط 10 رقم وارد کنید.

## توجه‌های مهم

⚠️ **این برنامه برای استفاده روی سیستم واقعی ویندوز طراحی شده است**
- برنامه در محیط Replit به دلیل محدودیت‌های headless mode کار نمی‌کند
- برای استفاده، فایل‌های پروژه را دانلود کرده و روی کامپیوتر ویندوز خود اجرا کنید

📝 **رفتار برنامه:**
- برنامه Chrome را به صورت visible باز می‌کند (نه headless)
- بعد از کلیک روی دکمه "ادامه"، مرورگر برای 30 ثانیه باز می‌ماند
- در این مدت می‌توانید کد تایید را به صورت دستی وارد کنید
- سپس مرورگر به طور خودکار بسته می‌شود

## رفع مشکلات (Troubleshooting)

### خطا: "Failed to load Eitaa website"
اگر با این پیام مواجه شدید:
```
Failed to load Eitaa website. The site may be blocking automated browsers or there is a network issue.
```

**راه‌حل‌ها:**
1. اتصال اینترنت خود را بررسی کنید
2. مطمئن شوید که به سایت ایتا دسترسی دارید (firewall یا فیلترشکن)
3. مرورگر کروم را به آخرین نسخه به‌روزرسانی کنید
4. برنامه را مجدداً اجرا کنید (گاهی اوقات در تلاش دوم موفق می‌شود)
5. اگر مشکل ادامه داشت، ممکن است سایت ایتا مرورگرهای خودکار را شناسایی کند

### برنامه هیچ کاری نمی‌کند
- مطمئن شوید Python و Chrome به درستی نصب شده‌اند
- Command Prompt/PowerShell را با دسترسی مدیریت (Administrator) اجرا کنید
- مسیر فایل را به درستی وارد کرده‌اید

### ChromeDriver دانلود نمی‌شود
- اتصال اینترنت را بررسی کنید
- اگر از proxy استفاده می‌کنید، تنظیمات آن را بررسی کنید

## ساختار پروژه

```
.
├── eitaa_automation.py   # فایل اصلی برنامه
├── requirements.txt      # لیست کتابخانه‌های مورد نیاز
├── setup.bat            # نصب خودکار برای ویندوز
├── setup.py             # نصب خودکار برای همه سیستم‌ها
└── README.md            # این فایل
```

## نحوه کار

1. برنامه ChromeDriver را به صورت خودکار دانلود و نصب می‌کند
2. مرورگر کروم را باز می‌کند
3. به سایت ایتا می‌رود
4. منتظر بارگذاری صفحه می‌ماند
5. فیلد شماره تلفن را پیدا می‌کند
6. شماره 9135621232 را وارد می‌کند
7. روی دکمه "ادامه" کلیک می‌کند
8. مرورگر را برای مدت زمان مشخص باز نگه می‌دارد

## رفع مشکلات

اگر برنامه با خطا مواجه شد:
- مطمئن شوید که مرورگر کروم نصب است
- اتصال اینترنت خود را بررسی کنید
- ممکن است نیاز باشد ChromeDriver به‌روزرسانی شود

## مجوز

این برنامه برای استفاده شخصی طراحی شده است.
